const rectangleLength: number = 0;
const rectangleWidth: number = 0;

function calcArea(rectangleLength: number, rectangleWidth: number): number {
  return rectangleLength * rectangleWidth;
}

console.log(calcArea(5, 7));
